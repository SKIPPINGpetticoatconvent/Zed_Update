# Zed Editor Auto Updater - Simplified Dependencies
# Core dependencies for the unified, simplified architecture

# GUI framework
PyQt5>=5.15.10
PyQt5-Qt5>=5.15.2

# HTTP requests
requests>=2.31.0

# System utilities  
psutil>=5.9.0
pywin32>=304; sys_platform == 'win32'

# Configuration (JSON format)
# No additional dependencies needed

# Build tools
pyinstaller>=5.0.0

# Core dependencies only - no dev/test tools